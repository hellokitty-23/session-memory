# Place this file at <project>/.codex/session-memory/config.toml
# Each key is a memory space name.
# Each value is a path relative to the project root.

[spaces]
"current-path" = "current-research"
"legacy-path" = "legacy-research"
